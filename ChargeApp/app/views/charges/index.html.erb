<p id="notice"><%= notice %></p>

<h1>Failed Charges</h1>

<table class="failed_charges">
  <thead>
    <tr>
      <th>Customer</th>
      <th>Amount</th>
      <th>Created</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @failed_charges.each do |charge| %>
      <tr>
        <td><%= charge.customer.name %></td>
        <td><%= charge.amount %></td>
        <td><%= charge.created %></td>
        <td><%= link_to 'Show', charge %></td>
        <td><%= link_to 'Edit', edit_charge_path(charge) %></td>
        <td><%= link_to 'Destroy', charge, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
</table>

<h1>Disputed Charges</h1>

<table class="disputed_charges">
  <thead>
    <tr>
      <th>Customer</th>
      <th>Amount</th>
      <th>Created</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @disputed_charges.each do |charge| %>
      <tr>
        <td><%= charge.customer.name %></td>
        <td><%= charge.amount %></td>
        <td><%= charge.created %></td>
        <td><%= link_to 'Show', charge %></td>
        <td><%= link_to 'Edit', edit_charge_path(charge) %></td>
        <td><%= link_to 'Destroy', charge, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
</table>

<h1>Successful Charges</h1>

<table class="successful_charges">
  <thead>
    <tr>
      <th>Customer</th>
      <th>Amount</th>
      <th>Created</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @successful_charges.each do |charge| %>
      <tr>
        <td><%= charge.customer.name %></td>
        <td><%= charge.amount %></td>
        <td><%= charge.created %></td>
        <td><%= link_to 'Show', charge %></td>
        <td><%= link_to 'Edit', edit_charge_path(charge) %></td>
        <td><%= link_to 'Destroy', charge, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
</table>

<br>

<%= link_to 'New Charge', new_charge_path %>
